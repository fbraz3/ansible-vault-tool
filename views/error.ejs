<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Ansible Vault Encryption / Decryption Tool - Error <%= typeof status !== 'undefined' ? status : '500' %></title>
    
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <!-- Font Awesome Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <!-- Custom Modern CSS -->
    <link rel='stylesheet' href='/css/style.css' />
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-modern fixed-top">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="fas fa-shield-alt me-2"></i>Ansible Vault Tool
            </a>
        </div>
    </nav>

    <div class="container mt-3 pt-3">
        <!-- Error Hero Section -->
        <div class="hero-section text-center mb-5">
            <div class="error-icon mb-4">
                <i class="fas fa-exclamation-triangle fa-5x text-warning"></i>
            </div>
            <h1 class="hero-title">
                <%= typeof status !== 'undefined' ? status : '500' %> Error
            </h1>
            <p class="hero-subtitle">
                <% if (typeof status !== 'undefined' && status == 404) { %>
                    Page not found
                <% } else { %>
                    Something went wrong
                <% } %>
            </p>
        </div>

        <!-- Error Details Card -->
        <div class="row justify-content-center">
            <div class="col-lg-8">
                <div class="card card-modern">
                    <div class="card-header card-header-modern text-center">
                        <i class="fas fa-info-circle me-2"></i>Error Details
                    </div>
                    <div class="card-body card-body-modern text-center">
                        <div class="mb-4">
                            <% if (typeof status !== 'undefined' && status == 404) { %>
                                <h3 class="mb-3">
                                    <i class="fas fa-search me-2 text-primary"></i>
                                    Page Not Found
                                </h3>
                                <p class="lead text-muted mb-4">
                                    The page you are looking for doesn't exist or has been moved.
                                </p>
                            <% } else { %>
                                <h3 class="mb-3">
                                    <i class="fas fa-bug me-2 text-danger"></i>
                                    <%= typeof message !== 'undefined' ? message : 'Internal Server Error' %>
                                </h3>
                                <p class="lead text-muted mb-4">
                                    An unexpected error occurred. Please try again later.
                                </p>
                                <% if (typeof error !== 'undefined' && error && error.stack) { %>
                                    <details class="mt-4">
                                        <summary class="btn btn-outline-secondary btn-sm">
                                            <i class="fas fa-code me-2"></i>Technical Details
                                        </summary>
                                        <pre class="mt-3 text-start bg-light p-3 rounded"><%= error.stack %></pre>
                                    </details>
                                <% } %>
                            <% } %>
                        </div>
                        
                        <div class="d-flex flex-column flex-md-row gap-3 justify-content-center">
                            <a href="/" class="btn btn-primary-modern btn-modern">
                                <i class="fas fa-home me-2"></i>Go to Home
                            </a>
                            <button onclick="history.back()" class="btn btn-secondary-modern btn-modern">
                                <i class="fas fa-arrow-left me-2"></i>Go Back
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Helpful Tips Section -->
        <div class="row mt-5">
            <div class="col-lg-4">
                <div class="card card-modern h-100">
                    <div class="card-body card-body-modern text-center">
                        <div class="feature-icon">
                            <i class="fas fa-shield-alt"></i>
                        </div>
                        <h5>Encrypt Data</h5>
                        <p class="text-muted">Secure your Ansible configurations with our encryption tool.</p>
                        <a href="/" class="btn btn-outline-primary btn-sm">Try Now</a>
                    </div>
                </div>
            </div>
            <div class="col-lg-4">
                <div class="card card-modern h-100">
                    <div class="card-body card-body-modern text-center">
                        <div class="feature-icon">
                            <i class="fas fa-unlock"></i>
                        </div>
                        <h5>Decrypt Data</h5>
                        <p class="text-muted">Safely decrypt your Ansible vault files online.</p>
                        <a href="/" class="btn btn-outline-primary btn-sm">Try Now</a>
                    </div>
                </div>
            </div>
            <div class="col-lg-4">
                <div class="card card-modern h-100">
                    <div class="card-body card-body-modern text-center">
                        <div class="feature-icon">
                            <i class="fas fa-api"></i>
                        </div>
                        <h5>API Access</h5>
                        <p class="text-muted">Integrate with our RESTful API for automation.</p>
                        <a href="/" class="btn btn-outline-primary btn-sm">Learn More</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <footer class="footer-modern">
        <div class="container">
            <div class="row">
                <div class="col-md-8">
                    <p class="mb-0">
                        <i class="fas fa-copyright me-2"></i>
                        Copyright © <%= new Date().getFullYear(); %> Ansible Vault Encryption/Decryption Tool
                    </p>
                </div>
                <div class="col-md-4 text-md-end">
                    <p class="mb-0">
                        <i class="fas fa-heart text-danger me-1"></i>
                        Made with love for DevOps
                    </p>
                </div>
            </div>
        </div>
    </footer>

    <!-- Bootstrap 5 JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <!-- jQuery -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js" crossorigin="anonymous"></script>
</body>

</html>